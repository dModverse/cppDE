% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solveODE.R
\name{batchAvailable}
\alias{batchAvailable}
\title{Will a Batch Run in Parallel?}
\usage{
batchAvailable(model)
}
\arguments{
\item{model}{A model handle from \code{\link[=cppODE]{cppODE()}} or \code{\link[=cvode]{cvode()}}.}
}
\value{
List with \code{symbol} (the batch entry resolved), \code{openmp} (cppDE was
built with OpenMP), \code{modelOpenmp} (this model's shared object was), and
\code{parallel} (all three). Two further fallbacks are decided at run time and
are not visible here: inside a forked child, and inside an enclosing
OpenMP region.
}
\description{
\code{solveODEBatch()} degrades to a serial loop without saying so. This reports
the conditions it depends on, so a caller can warn instead of silently
losing its threads.
}
\seealso{
\code{\link[=solveODEBatch]{solveODEBatch()}}
}
