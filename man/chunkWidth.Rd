% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tools.R
\name{chunkWidth}
\alias{chunkWidth}
\title{How many tangent directions a second-order model should carry at once}
\usage{
chunkWidth(
  n_states,
  n_steps = 2000,
  q_max = 5L,
  budget = getOption("cppDE.storeBudget", 2.56e+08),
  min_width = 8L,
  max_width = 256L
)
}
\arguments{
\item{n_states}{Number of state variables.}

\item{n_steps}{Expected number of accepted steps. The store holds one
checkpoint per step per condition, so this is the term that dominates.}

\item{q_max}{Highest method order, 5 for BDF and 12 for Adams.}

\item{budget}{Bytes the checkpoint store of one condition may take. Divide
the machine's budget by the number of conditions and forked workers that
will hold one at the same time.}

\item{min_width, max_width}{Bounds on the answer.}
}
\value{
One integer, the number of directions.
}
\description{
Second order fixes the width of the AD slab when the model is compiled, so a
parameter set wider than that is answered in blocks of directions. Since the
step sequence of \code{derivMode = "forward-reverse"} does not depend on the
width, the blocks all ride one grid and the width is a memory and cache
question rather than an accuracy one. Two bounds decide it: the checkpoint
store, which is what actually runs out, and the L2 cache the step arithmetic
sweeps.
}
