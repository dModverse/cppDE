% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tools.R
\name{compile}
\alias{compile}
\title{Compile Generated C++ Model Code}
\usage{
compile(..., output = NULL, args = NULL, cores = 1, verbose = FALSE)
}
\arguments{
\item{...}{One or more objects returned by \code{\link[=cppODE]{cppODE()}} or \code{\link[=funCpp]{funCpp()}}
that carry a \code{"srcfile"} attribute.}

\item{output}{Optional base name for a combined shared library.}

\item{args}{Optional additional compiler or linker arguments appended
after the flags from \code{"compileArgs"}.}

\item{cores}{Number of parallel compilation jobs (Unix only).}

\item{verbose}{Logical; if \code{TRUE}, show compiler commands.}
}
\value{
Invisibly returns \code{TRUE} on successful compilation.
}
\description{
Compiles one or more C++ source files generated by \code{\link[=cppODE]{cppODE()}} or
\code{\link[=funCpp]{funCpp()}} into shared libraries (\code{.so} or \code{.dll}) using
\verb{R CMD SHLIB}. Compile-time flags stored in each object's
\code{"compileArgs"} attribute are picked up automatically; further flags
may be passed through \code{args}.
}
\section{Environment variables}{

\code{CPPDE_EXTRA_CXXFLAGS} is appended to the compiler flags of every
model compiled in the session, after \code{"compileArgs"} and \code{args}. Use
it to apply a define, an \code{-march} setting or a sanitizer to a whole
run without touching the call sites. Exporting \code{PKG_CXXFLAGS} has no
effect: \code{compile()} sets that variable itself.

\code{CPPDE_EXTRA_LIBS} is the linker counterpart, appended last to
\code{PKG_LIBS}. A sanitizer needs both: \code{CPPDE_EXTRA_CXXFLAGS} and
\code{CPPDE_EXTRA_LIBS} set to \code{-fsanitize=thread} builds and links the
generated models under ThreadSanitizer.
}

